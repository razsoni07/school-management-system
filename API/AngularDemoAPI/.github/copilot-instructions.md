# Copilot Instructions

## General Guidelines
- Use `int?` for audit user identifiers (CreatedBy/UpdatedBy).
- Use `DateTime.UtcNow` for audit timestamps.
- Standardize `BaseEntity` and entity classes to use these types.

## Code Style
- Follow specific formatting rules.
- Maintain consistent naming conventions.